var Brick = require('../../../../../');
var Paintings = require('paintings');

module.exports = Brick(function (content) {
  content.brick.bind({
    '.paintings-container': Paintings.New()
  });
});
